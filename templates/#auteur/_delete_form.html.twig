<form method="post" action="{{ path('app_autor_test_delete', {'id': auteurs.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auteurs.id) }}">
    <button class="btn">Delete</button>
</form>
