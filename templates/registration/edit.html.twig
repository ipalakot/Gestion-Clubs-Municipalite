{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    
    {% include("menus/navigation_admin.html.twig") %}
    {% include("headers/header_admin.html.twig") %}


<div class="container">
     <article>
        <h2> Edition: {{user.login}}</h2> <br>                    
                        
        <div>
            {{ include('registration/_form.html.twig', {'button_label': 'Editer'}) }}

            {# <a href="{{ path('user_index') }}">Liste des User</a> #}
         </div> 
          {{ include('registration/_delete_form.html.twig') }}   
          <br><br><br><br><br><br><br>
        </div>
    </article>   
           
    </div>

{% endblock %}






