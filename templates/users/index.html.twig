{% extends 'base.html.twig' %}

{% block body %}

        {% include("menus/navigation_admin.html.twig") %}
        {% include("headers/header_admin.html.twig") %}

    <div class ="col-lg-12 bg-light">
        <div class ="container-sm">   <div>
                <h3> Gestion des utilisateurs </h3>
                <hr>
        </div>

         <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>noms</th>
                <th>Prenoms</th>
                <th>Adresse</th>
                <th>Mails</th> 
                <th>Tel.</th>
                <th>Action</th>
            </tr>
        </thead>

        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.noms }}</td>
                    <td>{{ user.prenoms }}</td>
                    <td>{{ user.adresse }}</td>
                    <td>{{ user.mail}}</td>
                    <td>{{ user.phone}}</td>
                    <td> 
                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                <div class="text-center">
                                    <a href="{{path('adm_users_display', {'id': user.id} ) }}" class="btn btn-primary"> Display /  </a> 
                                    <a href="{{path('adm_users_edit', {'id': user.id} ) }}" class="btn btn-primary"> Edit /  </a> 
                                    {{ include('users/_delete_form.html.twig') }}   
                                    {#<a href="{{path('adm_users_suppr', {'id': user.id} ) }}" class="btn btn-primary"> Suppr /  </a> #}
                                </div>
                            </div> 
                    
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Aucun article pour lemoment !</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
<a href="{{path('adm_users_new')}}" class="btn btn-primary"> Ajouter 1 Utilisateur /  </a> <br><br><br><br
    </div></div></div>


{% endblock %}




