<form method="post" action="{{ path('adm_articles_suppr', {'id': article.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn">suppression</button>
</form>
