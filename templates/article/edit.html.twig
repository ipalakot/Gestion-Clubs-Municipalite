{% extends 'base.html.twig' %}

{% block title %}Edition Article{% endblock %}

{% block body %}

    {% include("menus/navigation_admin.html.twig") %}
    {% include("headers/header_admin.html.twig") %}


<div class="container">
     <article>
        <h2> Edition: {{article.titre}}</h2> <br>                    
                        
        <div>    
            {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="container">
            <div class="row justify-content-md-center">
                <div class="col-md-auto">
                   <a href="{{ path('app_article') }}" class="btn btn-secondary">Liste des articles</a>
                </div>
                
                <div class="col col-lg-2">
                    {{ include('article/_delete_form.html.twig') }}
                </div>
            </div>               
        </div>
    </article> 
    </div>
{% endblock %}
