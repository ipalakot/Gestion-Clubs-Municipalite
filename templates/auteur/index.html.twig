{% extends 'base.html.twig' %}

{% block body %}

    {% include("menus/navigation.html.twig") %}
    {% include("headers/header.html.twig") %}

    <div class="container">
        <!-- Article main content -->
        <article class="col-md-12 maincontent">
            <div class="container">
                <div class="row">	
                    <h1>Liste des Usagers </h1>
                        <table class="table  table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Noms</th>
                                    <th>Prénoms</th>
                                    <th>Adresse</th>
                                    <th>Mails</th> 
                                    <th>Phone</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for auteur in auteurs %}
                                    <tr>
                                        <td>{{ auteur.id }}</td>
                                        <td>{{ auteur.noms }}</td>
                                        <td>{{ auteur.prenoms }}</td>
                                        <td>{{ auteur.adresse }}</td>
                                        <td>{{ auteur.mail }}</td>
                                        <td>{{ auteur.Phone}}</td> 
                                        <td> 
                                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                                <div class="text-center">
                                                    <a href="{{path('app_auteur_affichage', {'id': auteur.id})}}" class="btn btn-primary"> Contacter </a> 
                                                </div>
                                            </div> 
                    </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="5">Aucun article pour lemoment !</td>
                                    </tr>
                                {% endfor %}
                        </tbody>

 

                    </table>

                    <br><br><br><br>
                </div>
            </div>
        </article>
    </div>
{% endblock %}