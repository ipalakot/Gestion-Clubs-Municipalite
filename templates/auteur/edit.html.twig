{% extends 'base.html.twig' %}

{% block title %}Edit Auteur{% endblock %}

{% block body %}

    {% include("menus/navigation_admin.html.twig") %}
    {% include("headers/header_admin.html.twig") %}

   
<div class="container">
    <article>
        <h4>Edition : {{auteur.noms}} </h4>

        <div>
            {{ include('auteur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        </div> 
            <div class="container">
                <div class="row justify-content-md-center">
                    <div class="col-md-auto">
                        <a href="{{ path('adm_index_auteur') }}" class="btn btn-primary" >Liste des Auteurs </a>
                    </div>
                    <div class="col col-lg-2">
                        {{ include('auteur/_delete_form.html.twig') }}<br><br>
                    </div>
                </div>               
            </div>
        </div>
    </article>   
           
</div>
{% endblock %}
